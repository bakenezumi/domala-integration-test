db {
  h2 {
    url = "jdbc:h2:mem:domala_it;DB_CLOSE_DELAY=-1"
    username = sa
  }
  mysql {
    url = "jdbc:mysql://localhost/mysql"
    username = root
    password = password
  }
  postgres {
    url = "jdbc:postgresql://localhost:5432/postgres"
    username = postgres
    password = password
  }
}
