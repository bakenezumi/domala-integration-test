db {
  mysql {
    url = "jdbc:mysql://localhost/mysql"
    username = root
    password = password
  }
  postgres {
    url = "jdbc:postgresql://localhost:5432/postgres"
    username = postgres
    password = password
  }
}
